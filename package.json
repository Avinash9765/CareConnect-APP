{
  "name": "careconnect-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run frontend-build",
    "frontend-build": "cd frontend && npm install && npm run build",
    "backend-install": "cd backend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "mongoose": "^7.0.0",
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.3",
    "mongodb-memory-server": "^11.1.0"
  }
}
